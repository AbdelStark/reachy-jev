# Contributing

Open an issue before changing state, question-bank, or motion contracts. Keep decisions deterministic and hardware side effects outside the package. Add regression tests for boundaries and failures; run the Python checks listed in [README.md](README.md) and the TypeScript tests when changing behavior shared between languages. Never commit credentials or identifying media or transcripts.
