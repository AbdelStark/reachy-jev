# Contributing

Open an issue before changing public state or motion contracts. Keep decisions deterministic and hardware side effects outside this package. Add regression tests for boundaries and failures, run `npm run check` and `npm test`, and include SDK versions in integration reports. Never commit credentials, raw audio, video, or identifying transcripts.
